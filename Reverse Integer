class Solution:
    def reverse(self, x: int) -> int:
        c=0
        d=-1 if (x<0) else 1
        x=abs(x)
        while(x>0):
            a=x%10
            if c > 2**31 // 10 or c < -2**31 // 10:
                return 0
            c=c*10+a
            x=x//10
        return c*d
