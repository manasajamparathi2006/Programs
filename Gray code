from typing import List

class Solution:
    def grayCode(self, n: int) -> List[int]:
        result = [0]
        for i in range(n):
            # Reflect the current result and prefix with 1 << i
            result += [x | (1 << i) for x in reversed(result)]
        return result
